package dao;

import model.AdminUser;

public interface AdminDao {
    AdminUser login(String username, String password, String pin);
    void updateBalance(int adminId, int newBalance);
    int getBalance(int adminId);
}
