package dao.impl;

import dao.ProductDao;
import model.Product;
import util.DbConnection;

import java.sql.*;
import java.util.ArrayList;
import java.util.List;

public class ProductDaoImpl implements ProductDao {

    private Product map(ResultSet rs) throws SQLException {
        Product p = new Product();
        p.setId(rs.getInt("id"));
        p.setCategoryName(rs.getString("category_name"));
        p.setProductName(rs.getString("product_name"));
        p.setShelfPrice(rs.getInt("shelf_price"));
        p.setStockQty(rs.getInt("stock_qty"));
        return p;
    }

    @Override
    public List<Product> findAll() {
        List<Product> list = new ArrayList<>();
        String sql = "SELECT id,category_name,product_name,shelf_price,stock_qty FROM product ORDER BY id DESC";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ResultSet rs = ps.executeQuery();
            while (rs.next()) list.add(map(rs));
        } catch (SQLException e) { e.printStackTrace(); }
        return list;
    }

    @Override
    public Product findByName(String productName) {
        String sql = "SELECT id,category_name,product_name,shelf_price,stock_qty FROM product WHERE product_name=?";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ps.setString(1, productName);
            ResultSet rs = ps.executeQuery();
            if (rs.next()) return map(rs);
        } catch (SQLException e) { e.printStackTrace(); }
        return null;
    }

    @Override
    public Product findById(int id) {
        String sql = "SELECT id,category_name,product_name,shelf_price,stock_qty FROM product WHERE id=?";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ps.setInt(1, id);
            ResultSet rs = ps.executeQuery();
            if (rs.next()) return map(rs);
        } catch (SQLException e) { e.printStackTrace(); }
        return null;
    }

    @Override
    public void insert(Product p) {
        String sql = "INSERT INTO product(category_name,product_name,shelf_price,stock_qty) VALUES(?,?,?,?)";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ps.setString(1, p.getCategoryName());
            ps.setString(2, p.getProductName());
            ps.setInt(3, p.getShelfPrice());
            ps.setInt(4, p.getStockQty());
            ps.executeUpdate();
        } catch (SQLException e) { e.printStackTrace(); }
    }

    @Override
    public void updateStock(int productId, int newQty) {
        String sql = "UPDATE product SET stock_qty=? WHERE id=?";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ps.setInt(1, newQty);
            ps.setInt(2, productId);
            ps.executeUpdate();
        } catch (SQLException e) { e.printStackTrace(); }
    }

    @Override
    public void updatePrice(int productId, int newPrice) {
        String sql = "UPDATE product SET shelf_price=? WHERE id=?";
        try {
            PreparedStatement ps = DbConnection.getDb().prepareStatement(sql);
            ps.setInt(1, newPrice);
            ps.setInt(2, productId);
            ps.executeUpdate();
        } catch (SQLException e) { e.printStackTrace(); }
    }
}
